# Build output
output/

# SSL certificates (contain private keys — NEVER commit)
cert/

# Start scripts (template — reads secrets from .env at runtime, safe to commit)
# opencti/v2_start_opencti.sh          # UN-IGNORED: no embedded secrets
# opencti-worker/v2_start_opencti_worker.sh # UN-IGNORED: no embedded secrets

# Offline deploy archives
opencti-app.tar.gz
opencti-infra.tar.gz
opencti-offline-deploy.tar.gz
files/opencti-bundle.tar.gz
files/opencti-source.tar.gz
files/opencti-app-package.tar.gz
files/python-deps.tar.gz

# Runtime tarballs (large binaries — download during build)
files/node-v*-linux-x64.tar.xz
files/python312.tar.gz
files/nodejs22.tar.gz

# OpenCTI platform — populated by v2_prepare_opencti.sh (large, from repo source)
opencti/build/
opencti/node_modules/
opencti/src/
opencti/config/
opencti/static/
opencti/script/
opencti/public/
opencti/client-python/
opencti/.pip-packages/
opencti/.python-venv/
opencti/package.json
opencti/.yarnrc.yml
opencti/logs/
opencti/.support/
opencti/telemetry/


# OpenCTI worker — populated by v2_prepare_opencti_worker.sh
opencti-worker/src/
opencti-worker/.pip-packages/
opencti-worker/.python-venv/

